/* runforever.c - runforever */

#include <xinu.h>

/*------------------------------------------------------------------------
 *  runforever  -  run forever in an infinite while-loop
 *------------------------------------------------------------------------
 */
void runforever(void)
{
	while(1);
}
